version: "3.9"
services:
  devops-bridge:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    environment:
      SERVICE_ENV: local
      SERVICE_PORT: 8000
